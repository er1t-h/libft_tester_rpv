[package]
name = "libft_rpv"
version = "0.1.0"
edition = "2021"
build = "src/build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[build-dependencies]
bindgen = "0.64.0"

[dependencies]
cty = "0.2.2"
libc = "0.2.139"
rusty-fork = "0.3.0"

[features]
bonus = []
verbose = []
